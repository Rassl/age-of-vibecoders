# Public origin of the deployed game, NO trailing slash. Baked into the link
# preview tags in index.html (og:url, og:image). Empty makes the image URL
# root-relative, which most chat apps resolve but Facebook/LinkedIn do not.
VITE_SITE_URL=
